init 3 python:
    def init_main_characters():
        characters = []
        characters.append(CharacterEntity(len(characters), "Charna", "charna"))
        characters.append(CharacterEntity(len(characters), "Reina", "reina"))
        characters.append(CharacterEntity(len(characters), "Belial", "belial"))
        characters.append(CharacterEntity(len(characters), "Lapin", "lapin"))
        characters.append(CharacterEntity(len(characters), "Chien", "chien"))       
